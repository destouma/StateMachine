# StateMachine
Implementation of a basic State Machine

events (enum)
states (enum)

state machine:
* name
* transitions
* init state
* final state

transition:
* name
* source state
* destination state
* event
* action (callback)
